server:
  port: 8082
spring:
  application:
    name: service-two

# Custom properties for inter-service communication
app:
  services:
    service-one:
      base-url: http://localhost:8081

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs
